# =============================================================================
# SpaceAx AI — dependensi aplikasi (tanpa PyTorch)
#
# PyTorch:
#   • TPU v5e-1 (Colab/GCE): pip install -r requirements-colab-tpu.txt
#                            atau bash scripts/install_tpu.sh
#   • GPU/CPU lokal:         pip install -r requirements-torch.txt
#                            lalu pip install -r requirements.txt
# =============================================================================

numpy>=1.24.0,<3.0.0
requests>=2.31.0,<3.0.0
beautifulsoup4>=4.12.0,<5.0.0
rich>=13.7.0,<15.0.0

# Pencarian internet (chat / learn)
ddgs>=9.14.0,<10.0.0
duckduckgo-search>=5.0.0,<8.0.0

# Tokenizer BPE (PyPI — hindari git clone di Colab)
tokenizers>=0.19.0,<0.24.0
