# =============================================================================
# SpaceAx AI — TPU v5e-1 (Google Cloud TPU VM / lingkungan non-Colab)
#
# JANGAN pakai file ini di Google Colab — gunakan requirements-colab-tpu.txt
# (Colab sudah menyediakan torch; memaksa torch 2.6 memecah torch_xla 2.9)
#
# Urutan:
#   pip install -r requirements.txt
#   pip install -r requirements-tpu.txt
# =============================================================================

-r requirements.txt

--extra-index-url https://storage.googleapis.com/libtpu-releases/index.html

# Versi di index libtpu (2025–2026): 2.8.x, 2.9.x — JANGAN batasi <2.6
torch_xla[tpu]>=2.8.0,<2.10.0
