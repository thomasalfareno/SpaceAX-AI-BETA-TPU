# =============================================================================
# SpaceAx AI — tambahan TPU v5e-1 (Google Colab / Cloud TPU / GCE)
#
# Prasyarat:
#   1. Runtime TPU aktif (Colab: Runtime → Ubah jenis runtime → TPU v5e-1)
#   2. Sudah: pip install -r requirements.txt
#   3. Lalu: pip install -r requirements-tpu.txt
#      atau: bash scripts/install_tpu.sh
#
# Dokumentasi lengkap: README.md → bagian "TPU v5e-1"
# =============================================================================

-r requirements.txt

# PyTorch/XLA — pasang SETELAH torch dari requirements.txt
# Index resmi libtpu (Google); versi minor disesuaikan otomatis jika perlu
--extra-index-url https://storage.googleapis.com/libtpu-releases/index.html
torch_xla[tpu]>=2.4.0,<2.6.0

# Opsional: percepat compile XLA (Colab sering sudah menyediakan libtpu di image)
# libtpu — biasanya ditarik sebagai dependensi torch_xla[tpu]
